# This file has been auto-generated by i3-config-wizard(1).
# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $alt Mod1
set $sup Mod4
font pango:monospace 8
new_window 1pixel

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork -c000000 -f
exec --no-startup-id nm-applet

set $fetch_workspace_to_output exec ~/script/move_i3_workspace_to_current_output.py $(i3 --get-socketpath)
exec --no-startup-id ~/script/autostart.sh

bar {
        status_command i3status
}

####################################################
# Window stuff

bindsym $alt+Left focus left
bindsym $alt+Down focus down
bindsym $alt+Up focus up
bindsym $alt+Right focus right
bindsym $alt+Shift+Left move left
bindsym $alt+Shift+Down move down
bindsym $alt+Shift+Up move up
bindsym $alt+Shift+Right move right

bindsym $alt+h split h
bindsym $alt+v split v
bindsym $alt+f fullscreen toggle
bindsym $alt+s layout stacking
bindsym $alt+w layout tabbed
bindsym $alt+e layout toggle split
bindsym $alt+Shift+space floating toggle
bindsym $alt+space focus mode_toggle
bindsym $alt+a focus parent
floating_modifier $alt
bindsym $alt+q kill

bindsym $alt+Shift+c reload
bindsym $alt+Shift+r restart

# exit i3 (logs you out of your X session)
#bindsym $alt+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym $alt+r mode "resize"

mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

####################################################
# Workspace stuff

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "🡼"
set $ws2 "🡹"
set $ws3 "🡽"
set $ws4 "🡸"
set $ws5 "🞧"
set $ws6 "🡺"
set $ws7 "🡿"
set $ws8 "🡻"
set $ws9 "🡾"

# switch to workspace
bindsym $sup+1 $fetch_workspace_to_output $ws1
bindsym $sup+2 $fetch_workspace_to_output $ws2
bindsym $sup+3 $fetch_workspace_to_output $ws3
bindsym $sup+4 $fetch_workspace_to_output $ws4
bindsym $sup+5 $fetch_workspace_to_output $ws5
bindsym $sup+6 $fetch_workspace_to_output $ws6
bindsym $sup+7 $fetch_workspace_to_output $ws7
bindsym $sup+8 $fetch_workspace_to_output $ws8
bindsym $sup+9 $fetch_workspace_to_output $ws9
bindsym $sup+KP_Home $fetch_workspace_to_output $ws1
bindsym $sup+KP_Up $fetch_workspace_to_output $ws2
bindsym $sup+KP_Prior $fetch_workspace_to_output $ws3
bindsym $sup+KP_Left $fetch_workspace_to_output $ws4
bindsym $sup+KP_Begin $fetch_workspace_to_output $ws5
bindsym $sup+KP_Right $fetch_workspace_to_output $ws6
bindsym $sup+KP_End $fetch_workspace_to_output $ws7
bindsym $sup+KP_Down $fetch_workspace_to_output $ws8
bindsym $sup+KP_Next $fetch_workspace_to_output $ws9

# move focused container to workspace
bindsym $sup+Shift+1 move container to workspace $ws1; $fetch_workspace_to_output $ws1
bindsym $sup+Shift+2 move container to workspace $ws2; $fetch_workspace_to_output $ws2
bindsym $sup+Shift+3 move container to workspace $ws3; $fetch_workspace_to_output $ws3
bindsym $sup+Shift+4 move container to workspace $ws4; $fetch_workspace_to_output $ws4
bindsym $sup+Shift+5 move container to workspace $ws5; $fetch_workspace_to_output $ws5
bindsym $sup+Shift+6 move container to workspace $ws6; $fetch_workspace_to_output $ws6
bindsym $sup+Shift+7 move container to workspace $ws7; $fetch_workspace_to_output $ws7
bindsym $sup+Shift+8 move container to workspace $ws8; $fetch_workspace_to_output $ws8
bindsym $sup+Shift+9 move container to workspace $ws9; $fetch_workspace_to_output $ws9
bindsym $sup+Shift+KP_7 move container to workspace $ws1; $fetch_workspace_to_output $ws1
bindsym $sup+Shift+KP_8 move container to workspace $ws2; $fetch_workspace_to_output $ws2
bindsym $sup+Shift+KP_9 move container to workspace $ws3; $fetch_workspace_to_output $ws3
bindsym $sup+Shift+KP_4 move container to workspace $ws4; $fetch_workspace_to_output $ws4
bindsym $sup+Shift+KP_5 move container to workspace $ws5; $fetch_workspace_to_output $ws5
bindsym $sup+Shift+KP_6 move container to workspace $ws6; $fetch_workspace_to_output $ws6
bindsym $sup+Shift+KP_1 move container to workspace $ws7; $fetch_workspace_to_output $ws7
bindsym $sup+Shift+KP_2 move container to workspace $ws8; $fetch_workspace_to_output $ws8
bindsym $sup+Shift+KP_3 move container to workspace $ws9; $fetch_workspace_to_output $ws9

bindsym $sup+x move workspace to output right

####################################################
# hotkey stuff

#volume
set $refresh_i3status killall -SIGUSR1 i3status
set $raise_volume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
set $lower_volume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioRaiseVolume $raise_volume
bindsym XF86AudioLowerVolume $lower_volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym --border --whole-window  $sup+button4 $raise_volume
bindsym --border --whole-window  $sup+button5 $lower_volume

#launchers
bindsym $sup+t exec i3-sensible-terminal
bindsym $sup+r exec dmenu_run
bindsym $sup+Return exec --no-startup-id i3-dmenu-desktop

#apps
bindsym $sup+w exec x-www-browser
bindsym $sup+e exec xdg-open ~
bindsym $sup+o exec pavucontrol
bindsym $sup+c exec speedcrunch
bindsym $sup+l exec loginctl lock-session
bindsym $sup+Delete exec gnome-system-monitor
